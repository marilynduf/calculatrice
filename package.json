{
  "name": "calculator-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:sass": "sass src/styles:dist/css --style=compressed",
    "watch:sass": "sass --watch src/styles:dist/css --no-source-map"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  },
  "dependencies": {
    "sass": "^1.93.2",
    "typescript": "^5.9.3"
  }
}
